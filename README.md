# Frontend-Bundle

Desplegar el repositorio dentro del dominio del servidor. La aplicación tiene como dominio interno "/app", por lo tanto el dominio del servidor debe tener una carpeta nombrada de esta forma donde se alojará el repositorio.
